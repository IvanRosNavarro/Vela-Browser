{
  "name": "@vela/main",
  "version": "0.0.0",
  "private": true,
  "description": "Vela — proceso principal de Electron",
  "main": "./dist/index.js",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@vela/shared": "workspace:*",
    "fractional-indexing": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "22.10.0",
    "electron": "42.0.0",
    "rimraf": "5.0.7",
    "typescript": "5.4.5",
    "vite": "5.4.21",
    "vitest": "^3"
  }
}
